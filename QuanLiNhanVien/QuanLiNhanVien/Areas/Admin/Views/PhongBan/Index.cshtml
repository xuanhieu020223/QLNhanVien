@model IEnumerable<QuanLiNhanVien.Models.PhongBan>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<h2 class="text-center">Phòng ban</h2>

<div class="container">
    <p>
        @Html.ActionLink("Thêm phòng ban", "Create", null, new { @class = "btn btn-success" })
    </p>
    <div class="table-responsive">
        <table class="table table-striped table-bordered">
            <thead class="thead-light-sky-blue">
                <tr>
                    <th>
                        IDPB
                    </th>
                    <th>
                        Tên phòng ban
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.IDPB)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.NamePB)
                        </td>
                        <td>
                            @Html.ActionLink("Sửa", "Edit", new { id = item.IDPB }, new { @class = "btn btn-primary btn-sm" }) |
                            @Html.ActionLink("Chi tiết", "Details", new { id = item.IDPB }, new { @class = "btn btn-info btn-sm" }) |
                            @Html.ActionLink("Xóa", "Delete", new { id = item.IDPB }, new { @class = "btn btn-danger btn-sm" })
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
<style>
    .thead-light-sky-blue {
        background-color: #87CEEB; /* Màu Light Sky Blue */
    }
</style>